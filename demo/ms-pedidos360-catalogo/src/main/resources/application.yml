server:
  port: 8082

spring:
  application:
    name: ms-pedidos360-catalogo
  datasource:
    url: jdbc:h2:mem:catalogo;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
