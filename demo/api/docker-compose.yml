services:
  bff:
    build: .
    container_name: entra-demo-bff
    ports:
      - "8080:8080"
    env_file:
      - .env.docker
    networks:
      - entra-demo-net

networks:
  entra-demo-net:
    external: true
